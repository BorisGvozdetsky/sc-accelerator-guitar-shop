enum APIRoute {
  Guitars = '/guitars',
}

enum ToastMessage {
  ServerFail = 'Server is not responding'
}

enum AppRoute {
  Main = '/',
  Product = '/guitars',
}

export {APIRoute, ToastMessage, AppRoute};
